import styles from '../styles/Calendar.module.css';

export default function Calendar () {
    //ask Anna and juliana how to go about importing a calendar
    return (
        <div id="calendar" className={styles.main}>Here we can import the VBA calendar</div>
    )
}
